import Link from 'next/link'
import { ArrowRight, CheckCircle2, Clock3, Eye, TrendingUp } from 'lucide-react'
import { AppShell, PageHeader } from '@/components/app-shell'
import { buyers, property, viewings } from '@/lib/bolig-data'

export default function Dashboard() { return <AppShell><PageHeader eyebrow="Oversikt" title="Salget ditt, samlet." copy="Her ser du det viktigste i prosessen rundt Eksempelgata 12." /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={TrendingUp} label="Estimert verdi" value={property.price} note="+3,2% siste måned" /><Stat icon={Eye} label="Annonsevisninger" value="1 248" note="+18% denne uken" /><Stat icon={Clock3} label="Interessenter" value="12" note="3 nye i dag" /><Stat icon={CheckCircle2} label="Annonsekvalitet" value="94 / 100" note="Klar for publisering" /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.4fr_0.8fr]"><section className="rounded-3xl border border-border bg-background p-6 sm:p-8"><div className="flex items-center justify-between"><div><p className="eyebrow">Salgsreise</p><h2 className="mt-2 text-2xl font-semibold tracking-tight">Du er nesten klar</h2></div><span className="font-mono text-sm text-primary">62%</span></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full w-[62%] rounded-full bg-primary" /></div><div className="mt-8 grid gap-4 sm:grid-cols-2">{['Boligdata er bekreftet','Annonse er kvalitetssikret','Publiser annonsen','Planlegg første visning'].map((item, i) => <div className="flex items-start gap-3" key={item}><div className={`mt-0.5 flex size-6 items-center justify-center rounded-full ${i < 2 ? 'bg-accent text-accent-foreground' : 'border border-border text-muted-foreground'}`}>{i < 2 ? <CheckCircle2 className="size-4" /> : <span className="text-xs">{i + 1}</span>}</div><div><p className="text-sm font-medium">{item}</p><p className="mt-1 text-xs text-muted-foreground">{i < 2 ? 'Ferdig' : 'Neste steg'}</p></div></div>)}</div><Link href="/app/buyers" className="mt-8 flex min-h-12 items-center justify-center rounded-full bg-primary text-sm font-medium text-primary-foreground">Se neste steg <ArrowRight className="ml-2 size-4" /></Link></section><section className="rounded-3xl bg-primary p-6 text-primary-foreground sm:p-8"><p className="eyebrow text-primary-foreground/60">Neste aktivitet</p><h2 className="mt-3 text-3xl font-semibold tracking-tight">Første visning</h2><p className="mt-3 text-sm leading-6 text-primary-foreground/70">Du har {viewings[0].guests} påmeldte til tirsdag.</p><div className="mt-8 flex items-center gap-4 border-t border-primary-foreground/15 pt-5"><div className="rounded-xl bg-accent px-3 py-2 text-center text-accent-foreground"><strong className="block text-xl">{viewings[0].date}</strong><span className="text-[10px]">JUN</span></div><div><p className="font-medium">{viewings[0].time}</p><p className="text-sm text-primary-foreground/60">{viewings[0].type}</p></div></div><Link href="/app/viewings" className="mt-8 flex min-h-12 items-center justify-center rounded-full border border-primary-foreground/20 text-sm font-medium">Administrer visning</Link></section></div><section className="mt-8 rounded-3xl border border-border bg-background p-6 sm:p-8"><div className="flex items-center justify-between"><div><p className="eyebrow">Siste kjøpere</p><h2 className="mt-2 text-2xl font-semibold tracking-tight">Følg opp interessen</h2></div><Link href="/app/buyers" className="text-sm font-medium text-primary">Se alle <ArrowRight className="ml-1 inline size-4" /></Link></div><div className="mt-6 divide-y divide-border">{buyers.slice(0, 3).map((buyer) => <div className="flex items-center justify-between gap-4 py-4" key={buyer.name}><div><p className="text-sm font-medium">{buyer.name}</p><p className="mt-1 text-xs text-muted-foreground">{buyer.note}</p></div><span className="rounded-full bg-secondary px-3 py-1 text-xs text-muted-foreground">{buyer.status}</span></div>)}</div></section></AppShell> }
function Stat({ icon: Icon, label, value, note }: any) { return <div className="rounded-2xl border border-border bg-background p-5"><Icon className="size-5 text-primary" /><p className="mt-8 text-xs text-muted-foreground">{label}</p><p className="mt-1 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-primary">{note}</p></div> }
